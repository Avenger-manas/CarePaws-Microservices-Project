server:
  port: 8086

spring:
  application:
    name: REPORT-SERVICE
  
  # FILE UPLOAD FIX: Proper indentation and limits
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  data:
    mongodb:
      uri: mongodb+srv://rastogimanas329:Ys54a4sUZbZpCxhP@cluster0.vl8euvm.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: carepaws-report-service
  
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:13.60.184.171} # IP updated for connectivity
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:dolpi}
    password: ${SPRING_RABBITMQ_PASSWORD:dolpi123}

internal:
  service:
    token: INTERNAl-NGO-AND-MUNICIPAL-SERVICE-TOKEN

eureka:
  instance:
    prefer-ip-address: true
    # Added instance-id to avoid registration conflicts
    instance-id: ${spring.application.name}:${random.value}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      # FIX: eureka-server ki jagah direct IP use kar rahe hain
      defaultZone: http://13.60.184.171:8761/eureka/

resilience4j:
  retry:
    instances:
      ngoRetry:
        maxAttempts: 3
        waitDuration: 300ms
      municipalRetry:
        maxAttempts: 3
        waitDuration: 300ms
      notificationRetry:
        maxAttempts: 3
        waitDuration: 300ms

  circuitbreaker:
    instances:
      ngoCB:
        failureRateThreshold: 50
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        waitDurationInOpenState: 15s
      municipalCB:
        failureRateThreshold: 50
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        waitDurationInOpenState: 15s
      notificationCB:
        failureRateThreshold: 50
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        waitDurationInOpenState: 15s
